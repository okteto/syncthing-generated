# Text files use LF line endings in this repository
*   text=auto
# Except the dependencies, which we leave alone
vendor/**   -text=auto
# Diffs on these files are meaningless
*.svg           -diff
lib/auto/gui.files.go filter=lfs diff=lfs merge=lfs -text
